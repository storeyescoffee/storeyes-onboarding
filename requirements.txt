# On Raspberry Pi OS (Bookworm) prefer apt — see README:
#   sudo apt install -y python3-fastapi python3-uvicorn python3-jinja2 python3-picamera2
# This file is for `pip install -r` on other platforms / in a venv.

fastapi
uvicorn[standard]
jinja2

# USB / UVC webcam backend only (CAMERA_BACKEND = "usb"):
#   imageio[ffmpeg]
#   simplejpeg
#
# Pi Camera backend (CAMERA_BACKEND = "picamera2") comes from apt:
#   sudo apt install -y python3-picamera2
